# Day 1 Report — Project bootstrap

Created a new Maven Spring Boot 3.3 project at `ReconciliationBackend` with Java 17. Added the planned starters (Web, Data JPA, MySQL, Validation, Lombok) and the empty package layout: `controller`, `service`, `repository`, `entity`, `dto`, and `config`.
